# DeepSeek Harness(dsh) —— 网页快捷关闭程序插件(dsh-shutdown)

> 根据以下提示词完成插件的设计和开发

## 需求总览

### 1. 网页端 ui 关闭按钮

在启动 dsh web 端之后，浏览器界面中右上角显示一个关闭按钮。当点击这个按钮时，将会触发关闭 dsh 的提示弹窗（提供不再显示按钮，点击后将来点击关闭按钮将不再弹出提示而直接关闭 dsh，可以在设置中重新开启），确认后关闭 dsh 标签页的功能。

### 2. 结束 dsh 进程

当在浏览器界面中点击关闭按钮时，会触发结束整个 dsh 进程的动作。该动作应当为安全的结束整个 dsh，即关闭 dsh 。

### 3. 总结

由于启动 dsh 是在终端中输入指令，然后通过默认端口启动后通过浏览器访问 web 界面。故这个插件干两件事：在图形界面中触发关闭事件时，在得到用户的确认后关闭浏览器中 dsh 的窗口，然后结束 dsh 的进程。

## 实现

该插件的实现，是在独立的git仓库目录中进行。需要能够独立出 dsh 源码，通过 dsh plugin 安装和管理。同时，插件的实现可以导入 dsh 源码提供的包，源码将会提供（正常情况下源码目录和该插件的开发目录同级），但是务必在单独的 bundle + profile 的条件下实现。

已知在不加其他拓展插件的情况下，打开 dsh web 界面右上角有一个下载 `Session 日志` 的按钮，我们需要创建的按钮位置应该和这个按钮对齐并排列在右边。整体上看，关闭按钮是处于整个界面的最右上角，按钮的旁边是下载 `Session 日志` 的按钮。

在前端实现关闭按钮时，按钮的样式和 `Session 日志` 保持相同，图标使用一个 svg 渲染的叉 `x`。

### 可能遇到的问题和解决方法

- 在插件正常运行的过程中，用户点击关闭 dsh 按钮后可能会出现浏览器标签页实际没有成功关闭的情况。这种情况下，确保浏览器页面渲染一个 dsh 已经关闭的画面。保证即使窗口没有正常关闭，也能及时提醒用户 dsh 进程已经结束。