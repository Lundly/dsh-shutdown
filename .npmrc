# Local development only: the @deepseek-ai/* peer packages are provided by the
# target dsh profile at runtime, not fetched from npm here. Type resolution for
# typecheck comes from the sibling deepseek-harness checkout via tsconfig paths
# and project references, so installing devDeps is enough.
auto-install-peers=false
